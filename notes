--> gpx_to_csv :Les méthodes child.tag et child.tag.endswith('atemp') que j'ai utilisées dans 
                le script font partie du module lxml.etree. Lorsque vous utilisez lxml pour 
                analyser un fichier XML, il renvoie un arbre d'éléments où chaque nœud 
                est représenté par un objet lxml.etree.Element.
                    child.tag : Cela renvoie la balise (tag) complète de l'élément, y compris l'espace de noms 
                                le cas échéant. Par exemple, <gpxtpx:atemp> pourrait être la valeur de child.tag.
                    child.tag.endswith('atemp') : Cela utilise la méthode endswith() pour vérifier si la balise 
                                se termine par 'atemp'. Si c'est le cas, la condition est vraie.   
                <extensions>
                    <gpxtpx:TrackPointExtension>
                        <gpxtpx:atemp>24</gpxtpx:atemp>
                        <gpxtpx:hr>102</gpxtpx:hr>
                    </gpxtpx:TrackPointExtension>
                </extensions>
                c'est du xml . langage de balisage utilisé pour structurer des données de manière 
                lisible par un être humain et machine.
                Le GPX est un format de fichier XML couramment utilisé pour stocker des données de trace GPS,
                telles que des itinéraires, des pistes et des points de repère.

